[project]
name = "spark-bot"
version = "0.1.0"
description = "The core automation engine for OpenSpark"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.61.0",
    "httpx>=0.28.1",
    "beautifulsoup4>=4.15.0",
    "lxml>=6.1.1",
    "pydantic>=2.13.0",
    "python-dotenv>=1.2.2",
    "loguru>=0.7.3",
]

[tool.uv]
package = false